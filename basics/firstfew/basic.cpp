// include the header required for basic input/output
#include <iostream>
// since the majority of code and functions used will be from the
// standard namespace, we will use the std namespace

using namespace std;

// the main function is where the actual execution of a program begins
// for successfull execution of the main function , the return int value should be 0
int main(int argc, char const *argv[])
{
    // printing a basic string to the standard output i.e cout
    cout << "Hello Ghost 👻👻" << endl;
    return 0;
}
